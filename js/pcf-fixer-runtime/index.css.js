// Runtime CSS side-effect shim.
// The host shell already loads the shared app stylesheet, so this module
// exists to satisfy the generated Vite entry without introducing a missing
// import.
export {};
